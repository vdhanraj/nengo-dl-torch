version = "1.0.0"

# Whether this is a development version (None = release)
dev = None

# Latest nengo version this release has been tested with
# Format: (major, minor, patch)
latest_nengo_version = (4, 1, 0)

version_info = tuple(int(x) for x in version.split("."))
